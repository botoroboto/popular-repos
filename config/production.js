/* istanbul ignore file */

module.exports = {
  // If we wanted, we could have something like this to make requests to a different API
  // based on current scope:
  // app: require('../app/pages/app/config/production'),
};
